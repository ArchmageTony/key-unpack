[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "key-unpack"
version = "0.1.0"
description = "Core and CLI archive password helper."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
key-unpack = "key_unpack.cli:main"
